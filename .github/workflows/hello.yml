#Goal: Say Hello and tell the title/Name of repo

#Start with naming your workflow
name: Hello

#when workflows run
on: 
  push:
      branches: [main]

#what workflow does
jobs:
#name for the jobs
  greet:
#where the job run
    runs-on:  ubuntu-latest
#the sequence of steps job does
    steps: 
    -name: say Hello
    -run: echo "Hello Dosto"
      - 
